# MariaDB Seed Data

This directory contains schema definitions and seed data for MariaDB dialect.

**To be added in torture-test steps:** MariaDB DDL scripts for tables, indexes, and constraints, plus initial data fixtures for torture test reference applications.

**Target:** mariadb service (localhost:3307, root user)
