# PostgreSQL Seed Data

This directory contains schema definitions and seed data for PostgreSQL dialect.

**To be added in torture-test steps:** PL/pgSQL DDL scripts for tables, indexes, and constraints, plus initial data fixtures for torture test reference applications.

**Target:** postgres service (localhost:5432, postgres user)
