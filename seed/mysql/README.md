# MySQL Seed Data

This directory contains schema definitions and seed data for MySQL dialect.

**To be added in torture-test steps:** MySQL DDL scripts for tables, indexes, and constraints, plus initial data fixtures for torture test reference applications.

**Target:** `torture-mysql` service (localhost:**3308**, root user) — see `docker-compose.yml`
