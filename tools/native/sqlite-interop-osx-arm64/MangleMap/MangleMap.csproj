<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <!-- Developer tooling, not shipped: it is not part of any solution build. -->
    <AssemblyName>manglemap</AssemblyName>
    <RootNamespace>Jaunty.Tools.MangleMap</RootNamespace>
  </PropertyGroup>

</Project>
