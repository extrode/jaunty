<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>netstandard2.0;net8.0;net10.0</TargetFrameworks>
		<ImplicitUsings>enable</ImplicitUsings>
		<NoWarn>$(NoWarn);CS1712;</NoWarn>
	</PropertyGroup>

	<PropertyGroup Condition="'$(TargetFramework)' == 'netstandard2.0'">
		<DefineConstants>AsyncInterfaces</DefineConstants>
		<ExcludeFromCoverage>true</ExcludeFromCoverage>
	</PropertyGroup>

	<Choose>
		<When Condition="$(DefineConstants.Contains('AsyncInterfaces'))">
			<ItemGroup>
				<PackageReference Include="Microsoft.Bcl.AsyncInterfaces" Version="10.0.10" />
				<PackageReference Include="Microsoft.CSharp" Version="4.7.0" />
			</ItemGroup>
		</When>
	</Choose>

	<PropertyGroup Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net8.0'))">
		<Nullable>enable</Nullable>
	</PropertyGroup>

	<ItemGroup>
		<ProjectReference Include="..\Jaunty\Jaunty.csproj" />
	</ItemGroup>

	<ItemGroup>
		<AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
			<_Parameter1>Jaunty.Fluent.Tests</_Parameter1>
		</AssemblyAttribute>
		<AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
			<_Parameter1>Jaunty.Fluent.SourceGen.Tests</_Parameter1>
		</AssemblyAttribute>
		<!-- The serial half of Jaunty.Fluent.Tests; mutates the global JauntyConfig statics. -->
		<AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleTo">
			<_Parameter1>Jaunty.Fluent.ConfigTests</_Parameter1>
		</AssemblyAttribute>
	</ItemGroup>

	<PropertyGroup>
		<PackageId>Extrode.Jaunty.Fluent</PackageId>
		<Authors>Extrode LLC</Authors>
		<Description>Type-safe fluent SQL query builder for Jaunty.</Description>
		<RepositoryUrl>https://github.com/extrode/jaunty</RepositoryUrl>
		<Nullable>enable</Nullable>
	</PropertyGroup>

</Project>
